-- Счётчик переложенных в папку картинок.
--
-- Отдельным файлом: применённые миграции отмечаются по имени файла, и правка
-- уже применённой не выполнится никогда (см. комментарий в runner.ts).
--
-- Без этого счётчика перекладывание файлов по папкам было полностью невидимым:
-- импортёр его считал, но до журнала и панели число не доезжало, и отличить
-- «не переложилось» от «переложилось молча» было нечем.

ALTER TABLE macro_sync_runs
  ADD COLUMN IF NOT EXISTS "imagesMoved" integer NOT NULL DEFAULT 0;
