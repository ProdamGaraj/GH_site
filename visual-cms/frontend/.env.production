# Vite mode "production" — prod site at the apex domain https://gh.uz/.
# Read by vite.config.ts via loadEnv(). Comma-separated list of hostnames that
# Vite's host-check must accept (localhost / *.localhost / IPs are always OK).
ALLOWED_HOSTS=gh.uz
